import React from "react";
import { Route, BrowserRouter } from "react-router-dom";
import CreateRoom from "./pages/CreateRoom";

export default function RoomFeature(props) {
  //   const match = useRouteMatch();
  console.log(props);
  return "";
  //   return (
  //     <BrowserRouter>
  //       <Route path={match.path} element={<CreateRoom />}></Route>
  //     </BrowserRouter>
  //   );
}
